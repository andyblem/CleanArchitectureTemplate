import { AuthenticationConstants } from './authentication-constants';

describe('AuthenticationConstants', () => {
  it('should create an instance', () => {
    expect(new AuthenticationConstants()).toBeTruthy();
  });
});
